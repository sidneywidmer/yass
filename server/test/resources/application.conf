environment = test

db {
  url = "jdbc:postgresql://localhost:5433/yass-test"
  username = postgres-test
  password = postgres-test
}

server {
  port = 8080
}

ory {
  basePath = "http://localhost:4434"
}
